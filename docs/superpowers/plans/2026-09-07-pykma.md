# pykma Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기상청 단기예보 API를 위경도 두 개로 부를 수 있게 만드는 Python SDK를 PyPI에 배포하고, 그 위에 MCP 서버를 얹는다.

**Architecture:** 네트워크를 모르는 순수 함수 계층(`grid`, `clock`, `codes`)이 아래에 있고, 그것들을 엮어 HTTP를 치는 `client`가 위에 있다. 사람들이 실제로 틀리는 곳(격자 변환·발표시각 계산)이 전부 순수 함수 쪽에 있으므로 네트워크 없이 전수 테스트된다. MCP 서버는 `pykma`에만 의존하고 반대 방향 의존은 없다.

**Tech Stack:** Python 3.11+, httpx, uv, ruff, ty, pytest, GitHub Actions, PyPI Trusted Publishing, MCP Python SDK

## Global Constraints

- Python **3.11+**. 런타임 의존성은 **`httpx` 하나**. MCP 층은 `pykma[mcp]` extra로 분리한다.
- **테스트는 네트워크를 치지 않는다.** 실제 응답은 녹화해 `tests/fixtures/`에 둔다. CI가 기상청 서버나 인증키에 기대면 안 된다.
- **커버리지 하한 90%.** `pytest --cov=pykma --cov-fail-under=90`.
- 게이트 네 개 전부 통과해야 커밋한다: `uv run ruff format` → `uv run ruff check` → `uv run ty check` → `uv run pytest`.
- **인증키를 코드·테스트·예외 메시지·로그에 절대 넣지 않는다.** 저장소에는 이름만 있는 `.env.example`만 둔다.
- `grid.py`와 `clock.py`는 **의존성 0**이다. `pykma` 내부 다른 모듈도 import하지 않는다 (`models`/`codes` 포함).
- `clock.py`는 내부에서 `datetime.now()`를 부르지 않는다. 호출 시각을 항상 인자로 받는다.
- **모르는 코드·형식은 버리지 않는다.** 파싱 실패한 값도 원문을 객체에 남긴다.
- 커밋 메시지는 `feat:` `fix:` `test:` `docs:` `chore:` `ci:` 접두사를 쓴다.
- 공개 API가 바뀌면 같은 커밋에서 README를 갱신한다.

---

## File Structure

| 파일 | 책임 | 의존 |
|---|---|---|
| `src/pykma/grid.py` | 위경도 ↔ 기상청 격자 좌표 | 없음 (`math`만) |
| `src/pykma/clock.py` | 오퍼레이션별 `base_date`/`base_time` 계산 | 없음 (`datetime`만) |
| `src/pykma/codes.py` | 카테고리 코드 → 타입, 강수/적설 문자열 파싱 | 없음 |
| `src/pykma/models.py` | `Observation`/`Forecast`, 평평한 행 → 시간대 묶기 | `codes` |
| `src/pykma/errors.py` | 예외 계층, 포털 결과코드 → 예외 매핑 | 없음 |
| `src/pykma/client.py` | HTTP 호출 (sync + async) | 위 전부 + `httpx` |
| `src/pykma/__init__.py` | 공개 API 재수출 | 위 전부 |
| `src/pykma_mcp/server.py` | MCP 도구 3개 | `pykma` |
| `tests/fixtures/*.json` | 녹화된 응답 | — |

---

## Task 1: 프로젝트 뼈대와 CI

**Files:**
- Create: `pyproject.toml`, `src/pykma/__init__.py`, `tests/test_smoke.py`,
  `.github/workflows/ci.yml`, `.env.example`, `README.md`

**Interfaces:**
- Consumes: 없음
- Produces: `pykma.__version__: str`

- [ ] **Step 1: uv로 프로젝트를 만든다**

```bash
cd /Users/gimgigang/project/pykma
uv init --lib --name pykma --python 3.11
```

`uv init --lib`가 `src/pykma/` 레이아웃을 만든다. 이미 있는 `docs/`와 `.gitignore`는 건드리지 않는다.

- [ ] **Step 2: `pyproject.toml`을 채운다**

```toml
[project]
name = "pykma"
version = "0.1.0"
description = "기상청 단기예보 API Python SDK — 위경도만 넣으면 된다"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "alton15" }]
keywords = ["기상청", "날씨", "weather", "korea", "kma", "forecast", "공공데이터"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Natural Language :: Korean",
    "Topic :: Scientific/Engineering :: Atmospheric Science",
]
dependencies = ["httpx>=0.27"]

[project.optional-dependencies]
mcp = ["mcp>=1.2"]

[project.urls]
Homepage = "https://github.com/alton15/pykma"
Issues = "https://github.com/alton15/pykma/issues"

[project.scripts]
pykma-mcp = "pykma_mcp.server:main"

[dependency-groups]
dev = ["pytest>=8", "pytest-cov>=5", "pytest-asyncio>=0.24", "ruff>=0.8", "ty>=0.0.1a1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pykma", "src/pykma_mcp"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.pytest.ini_options]
addopts = "--cov=pykma --cov=pykma_mcp --cov-report=term-missing --cov-fail-under=90"
asyncio_mode = "auto"
testpaths = ["tests"]
```

`[project.scripts]`의 `pykma-mcp`는 Task 8에서 구현한다. 그때까지는 `uvx` 실행만 실패하고 라이브러리 테스트에는 영향이 없다.

- [ ] **Step 3: 패키지 초기 파일과 스모크 테스트를 쓴다**

`src/pykma/__init__.py`:

```python
"""기상청 단기예보 API Python SDK."""

__version__ = "0.1.0"

__all__ = ["__version__"]
```

`tests/test_smoke.py`:

```python
import pykma


def test_버전이_노출된다():
    # given / when
    version = pykma.__version__

    # then
    assert version.count(".") == 2
```

- [ ] **Step 4: 게이트 네 개를 돌린다**

```bash
uv sync --all-extras
uv run ruff format
uv run ruff check
uv run ty check
uv run pytest
```

Expected: 넷 다 통과. 커버리지는 파일이 `__init__.py` 하나뿐이라 100%.

- [ ] **Step 5: `.env.example`과 README 뼈대를 쓴다**

`.env.example` — **값은 절대 넣지 않는다. 이름만 둔다.**

```
KMA_SERVICE_KEY=
```

`README.md`:

````markdown
# pykma

기상청 단기예보 API Python SDK. 위경도만 넣으면 된다.

```python
from pykma import KMA

kma = KMA(service_key="...")
print(kma.now(37.5665, 126.9780).temperature)
```

## 설치

```bash
pip install pykma
```

## 인증키

[공공데이터포털](https://www.data.go.kr/data/15084084/openapi.do)에서 **단기예보 조회서비스**를
활용신청한다. 승인에 하루 정도 걸린다. 발급된 키는 `KMA(service_key=...)`로 넘기거나
환경변수 `KMA_SERVICE_KEY`에 둔다.
````

- [ ] **Step 6: CI를 건다**

`.github/workflows/ci.yml`:

```yaml
name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv sync --all-extras
      - run: uv run ruff format --check
      - run: uv run ruff check
      - run: uv run ty check
      - run: uv run pytest
```

- [ ] **Step 7: GitHub 저장소를 만들고 푸시한다**

```bash
gh repo create alton15/pykma --public \
  --description "기상청 단기예보 API Python SDK — 위경도만 넣으면 된다"
git remote add origin https://github.com/alton15/pykma.git
git add -A
git commit -m "chore: 프로젝트 뼈대와 CI"
git push -u origin main
gh run watch
```

Expected: CI 초록.

---

## Task 2: `grid.py` — 위경도 ↔ 격자

기상청 API는 위경도를 받지 않는다. Lambert Conformal Conic 격자 `(nx, ny)`만 받는다.
사람들이 이 변환 함수를 블로그에서 복붙하는 것이 고통 #1이다.

**Files:**
- Create: `src/pykma/grid.py`, `tests/test_grid.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `Grid` — `@dataclass(frozen=True)`, 필드 `nx: int`, `ny: int`
  - `to_grid(lat: float, lon: float) -> Grid`
  - `to_latlon(grid: Grid) -> tuple[float, float]`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_grid.py`:

```python
import pytest

from pykma.grid import Grid, to_grid, to_latlon

# 기상청이 공개한 「기상청41_단기예보 조회서비스_오픈API활용가이드」 부속
# 지점 좌표 엑셀에서 뽑은 기준점. 블로그의 동 중심좌표를 쓰면 안 된다 —
# 격자가 5km라 동 중심점은 경계 어느 쪽에도 떨어질 수 있다.
ANCHORS = [
    ("서울특별시청", 37.5665, 126.9780, 60, 127),
    ("부산광역시청", 35.1796, 129.0756, 98, 76),
    ("제주특별자치도청", 33.4996, 126.5312, 53, 38),
]


@pytest.mark.parametrize(("name", "lat", "lon", "nx", "ny"), ANCHORS)
def test_공식_좌표표의_기준점을_재현한다(name, lat, lon, nx, ny):
    # given / when
    grid = to_grid(lat, lon)

    # then
    assert (grid.nx, grid.ny) == (nx, ny), name


@pytest.mark.parametrize(("name", "lat", "lon", "nx", "ny"), ANCHORS)
def test_격자에서_되돌린_위경도가_같은_격자로_돌아온다(name, lat, lon, nx, ny):
    # given
    grid = Grid(nx, ny)

    # when
    back_lat, back_lon = to_latlon(grid)

    # then — 격자 중심으로 되돌아오므로 원래 좌표와는 다르지만, 다시 변환하면 같은 칸이다
    assert to_grid(back_lat, back_lon) == grid, name


def test_격자는_불변이다():
    # given
    grid = to_grid(37.5665, 126.9780)

    # when / then
    with pytest.raises(AttributeError):
        grid.nx = 1  # type: ignore[misc]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `uv run pytest tests/test_grid.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'pykma.grid'`

- [ ] **Step 3: 구현한다**

`src/pykma/grid.py`:

```python
"""위경도와 기상청 예보 격자 사이의 변환.

기상청 API는 위경도를 받지 않는다. Lambert Conformal Conic 투영으로 만든
5km 격자의 정수 좌표 (nx, ny)만 받는다. 아래 상수는 기상청이 활용가이드와
함께 배포하는 변환 예제(dfs_xy_conv)의 값 그대로다.

이 모듈은 아무것도 import하지 않는다(math 제외). 네트워크도, 같은 패키지의
다른 모듈도 모른다. 사람들이 가장 많이 틀리는 곳이라 네트워크 없이
전수 테스트되어야 한다.
"""

import math
from dataclasses import dataclass

__all__ = ["Grid", "to_grid", "to_latlon"]

_RE = 6371.00877  # 지구 반경 (km)
_GRID = 5.0  # 격자 간격 (km)
_SLAT1 = 30.0  # 투영 위도 1 (도)
_SLAT2 = 60.0  # 투영 위도 2 (도)
_OLON = 126.0  # 기준점 경도 (도)
_OLAT = 38.0  # 기준점 위도 (도)
_XO = 43  # 기준점 X좌표 (격자)
_YO = 136  # 기준점 Y좌표 (격자)

_DEGRAD = math.pi / 180.0
_RADDEG = 180.0 / math.pi


@dataclass(frozen=True)
class Grid:
    """기상청 예보 격자의 한 칸."""

    nx: int
    ny: int


def _projection() -> tuple[float, float, float]:
    """투영 상수 (sn, sf, ro). 위경도와 무관하므로 한 번만 계산한다."""
    re = _RE / _GRID
    slat1 = _SLAT1 * _DEGRAD
    slat2 = _SLAT2 * _DEGRAD
    olat = _OLAT * _DEGRAD

    sn = math.tan(math.pi * 0.25 + slat2 * 0.5) / math.tan(math.pi * 0.25 + slat1 * 0.5)
    sn = math.log(math.cos(slat1) / math.cos(slat2)) / math.log(sn)

    sf = math.tan(math.pi * 0.25 + slat1 * 0.5)
    sf = sf**sn * math.cos(slat1) / sn

    ro = math.tan(math.pi * 0.25 + olat * 0.5)
    ro = re * sf / ro**sn

    return sn, sf, ro


_SN, _SF, _RO = _projection()


def to_grid(lat: float, lon: float) -> Grid:
    """위경도를 격자 좌표로 옮긴다."""
    re = _RE / _GRID
    olon = _OLON * _DEGRAD

    ra = math.tan(math.pi * 0.25 + lat * _DEGRAD * 0.5)
    ra = re * _SF / ra**_SN

    theta = lon * _DEGRAD - olon
    if theta > math.pi:
        theta -= 2.0 * math.pi
    if theta < -math.pi:
        theta += 2.0 * math.pi
    theta *= _SN

    # +0.5 후 int()는 반올림이다. 기상청 예제 코드와 같은 방식으로 맞춘다.
    return Grid(
        nx=int(ra * math.sin(theta) + _XO + 0.5),
        ny=int(_RO - ra * math.cos(theta) + _YO + 0.5),
    )


def to_latlon(grid: Grid) -> tuple[float, float]:
    """격자 좌표를 그 칸의 중심 위경도로 되돌린다."""
    olon = _OLON * _DEGRAD

    xn = grid.nx - _XO
    yn = _RO - grid.ny + _YO
    ra = math.sqrt(xn * xn + yn * yn)
    if _SN < 0.0:
        ra = -ra

    alat = (_RE / _GRID * _SF / ra) ** (1.0 / _SN)
    alat = 2.0 * math.atan(alat) - math.pi * 0.5

    if abs(xn) <= 0.0:
        theta = 0.0
    elif abs(yn) <= 0.0:
        theta = math.pi * 0.5
        if xn < 0.0:
            theta = -theta
    else:
        theta = math.atan2(xn, yn)

    alon = theta / _SN + olon

    return alat * _RADDEG, alon * _RADDEG
```

- [ ] **Step 4: 통과를 확인한다**

Run: `uv run pytest tests/test_grid.py -v`
Expected: PASS (7건 — 파라미터 3개 × 2 + 불변 1건)

- [ ] **Step 5: 기준점을 늘린다**

공공데이터포털의 단기예보 조회서비스 페이지에서 「기상청41_단기예보 조회서비스_오픈API활용가이드」
첨부의 지점 좌표 엑셀을 내려받아, 광역시·도청 소재지 **최소 15곳**을 `ANCHORS`에 추가한다.
**엑셀의 위경도와 nx/ny를 그대로 옮긴다.** 블로그나 검색 결과의 값을 쓰지 않는다.

Run: `uv run pytest tests/test_grid.py -v`
Expected: 전부 PASS. 하나라도 어긋나면 상수가 아니라 **옮겨 적은 값**을 먼저 의심한다
(엑셀에 위경도가 도분초로 적힌 열과 십진수로 적힌 열이 따로 있다).

- [ ] **Step 6: 게이트를 돌리고 커밋한다**

```bash
uv run ruff format && uv run ruff check && uv run ty check && uv run pytest
git add src/pykma/grid.py tests/test_grid.py
git commit -m "feat: 위경도와 기상청 격자 사이 변환"
```

---

## Task 3: `clock.py` — `base_date` / `base_time` 계산

세 오퍼레이션의 발표 주기와 제공 지연이 전부 다르고, 자정 근처에서 날짜가 하루 뒤로
넘어간다. 사람들이 두 번째로 많이 틀리는 곳이자 고통 #2다.

**Files:**
- Create: `src/pykma/clock.py`, `tests/test_clock.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `Operation` — `StrEnum`. 멤버 `ULTRA_NCST`/`ULTRA_FCST`/`VILAGE_FCST`,
    값은 각각 `"getUltraSrtNcst"`/`"getUltraSrtFcst"`/`"getVilageFcst"`
  - `BaseTime` — `@dataclass(frozen=True)`, 필드 `date: str` (`YYYYMMDD`), `time: str` (`HHMM`)
  - `base_for(operation: Operation, now: datetime) -> BaseTime`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_clock.py`:

```python
from datetime import datetime

import pytest

from pykma.clock import BaseTime, Operation, base_for


@pytest.mark.parametrize(
    ("now", "expected"),
    [
        # 14시 발표는 14:10부터 제공된다. 그 전에는 11시 발표가 최신이다.
        (datetime(2026, 9, 7, 14, 9), BaseTime("20260907", "1100")),
        (datetime(2026, 9, 7, 14, 10), BaseTime("20260907", "1400")),
        (datetime(2026, 9, 7, 14, 11), BaseTime("20260907", "1400")),
        # 자정 넘김: 23시 발표가 다음 날 02:10까지 최신이다.
        (datetime(2026, 9, 8, 0, 0), BaseTime("20260907", "2300")),
        (datetime(2026, 9, 8, 2, 9), BaseTime("20260907", "2300")),
        (datetime(2026, 9, 8, 2, 10), BaseTime("20260908", "0200")),
        # 발표 직후 경계 전부
        (datetime(2026, 9, 7, 5, 10), BaseTime("20260907", "0500")),
        (datetime(2026, 9, 7, 8, 10), BaseTime("20260907", "0800")),
        (datetime(2026, 9, 7, 11, 10), BaseTime("20260907", "1100")),
        (datetime(2026, 9, 7, 17, 10), BaseTime("20260907", "1700")),
        (datetime(2026, 9, 7, 20, 10), BaseTime("20260907", "2000")),
        (datetime(2026, 9, 7, 23, 10), BaseTime("20260907", "2300")),
    ],
)
def test_단기예보는_8회_발표에_10분_지연을_더한다(now, expected):
    # given / when
    result = base_for(Operation.VILAGE_FCST, now)

    # then
    assert result == expected


@pytest.mark.parametrize(
    ("now", "expected"),
    [
        # 정시 발표 + 40분 제공.
        (datetime(2026, 9, 7, 14, 39), BaseTime("20260907", "1300")),
        (datetime(2026, 9, 7, 14, 40), BaseTime("20260907", "1400")),
        # 자정 넘김
        (datetime(2026, 9, 8, 0, 0), BaseTime("20260907", "2300")),
        (datetime(2026, 9, 8, 0, 40), BaseTime("20260908", "0000")),
    ],
)
def test_초단기실황은_정시_발표에_40분_지연을_더한다(now, expected):
    # given / when
    result = base_for(Operation.ULTRA_NCST, now)

    # then
    assert result == expected


@pytest.mark.parametrize(
    ("now", "expected"),
    [
        # 매시 30분 발표 + 45분 제공 = 매시 15분부터 직전 시각의 30분 발표가 유효.
        (datetime(2026, 9, 7, 14, 14), BaseTime("20260907", "1230")),
        (datetime(2026, 9, 7, 14, 15), BaseTime("20260907", "1330")),
        (datetime(2026, 9, 7, 15, 14), BaseTime("20260907", "1330")),
        # 자정 넘김
        (datetime(2026, 9, 8, 0, 14), BaseTime("20260907", "2230")),
        (datetime(2026, 9, 8, 0, 15), BaseTime("20260907", "2330")),
    ],
)
def test_초단기예보는_30분_발표에_45분_지연을_더한다(now, expected):
    # given / when
    result = base_for(Operation.ULTRA_FCST, now)

    # then
    assert result == expected


def test_오퍼레이션_값은_API_경로와_같다():
    # given / when / then — client가 이 값을 URL에 그대로 붙인다
    assert Operation.VILAGE_FCST == "getVilageFcst"
    assert Operation.ULTRA_NCST == "getUltraSrtNcst"
    assert Operation.ULTRA_FCST == "getUltraSrtFcst"
```

- [ ] **Step 2: 실패를 확인한다**

Run: `uv run pytest tests/test_clock.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'pykma.clock'`

- [ ] **Step 3: 구현한다**

`src/pykma/clock.py`:

```python
"""오퍼레이션별 base_date / base_time 계산.

기상청은 예보를 정해진 시각에 발표하고, 발표한 자료를 API로 내보내기까지
얼마간 지연이 있다. 발표 시각을 base_time으로 넣어야 하는데 아직 지연이
안 지났으면 자료가 없다. 그래서 "지금 부를 수 있는 가장 최근 발표"를
골라야 하고, 그 계산이 자정을 넘으면 날짜까지 하루 전으로 내려간다.

| 오퍼레이션 | 발표 | API 제공 |
|---|---|---|
| 초단기실황 | 매시 정시 | 발표 + 40분 |
| 초단기예보 | 매시 30분 | 발표 + 45분 |
| 단기예보 | 02·05·08·11·14·17·20·23시 | 발표 + 10분 |

이 모듈은 내부에서 datetime.now()를 부르지 않는다. 호출 시각을 항상 인자로
받으므로 경계를 네트워크도 시계 조작도 없이 전수 테스트할 수 있다.
"""

from dataclasses import dataclass
from datetime import datetime, timedelta
from enum import StrEnum

__all__ = ["BaseTime", "Operation", "base_for"]


class Operation(StrEnum):
    """단기예보 조회서비스의 오퍼레이션. 값이 곧 API 경로의 마지막 조각이다."""

    ULTRA_NCST = "getUltraSrtNcst"
    ULTRA_FCST = "getUltraSrtFcst"
    VILAGE_FCST = "getVilageFcst"


@dataclass(frozen=True)
class BaseTime:
    """API에 넘길 발표 시각."""

    date: str
    """YYYYMMDD"""
    time: str
    """HHMM"""


# 단기예보 발표 시각 (시)
_VILAGE_HOURS = (2, 5, 8, 11, 14, 17, 20, 23)

# 오퍼레이션별 제공 지연
_DELAY = {
    Operation.ULTRA_NCST: timedelta(minutes=40),
    Operation.ULTRA_FCST: timedelta(minutes=45),
    Operation.VILAGE_FCST: timedelta(minutes=10),
}


def base_for(operation: Operation, now: datetime) -> BaseTime:
    """`now` 시점에 부를 수 있는 가장 최근 발표 시각."""
    # 지연을 먼저 빼면, 남은 것은 "이미 제공이 시작된 발표"들의 시간대가 된다.
    # 이렇게 하면 자정 넘김이 datetime 산술 하나로 처리된다.
    available = now - _DELAY[operation]

    if operation is Operation.ULTRA_NCST:
        issued = available.replace(minute=0, second=0, microsecond=0)
    elif operation is Operation.ULTRA_FCST:
        # 30분 발표라 available의 분이 30 미만이면 직전 시각의 30분이다.
        issued = available.replace(minute=30, second=0, microsecond=0)
        if available.minute < 30:
            issued -= timedelta(hours=1)
    else:
        issued = _latest_vilage(available)

    return BaseTime(date=issued.strftime("%Y%m%d"), time=issued.strftime("%H%M"))


def _latest_vilage(available: datetime) -> datetime:
    """`available` 이하의 가장 늦은 단기예보 발표 시각."""
    for hour in reversed(_VILAGE_HOURS):
        if hour <= available.hour:
            return available.replace(hour=hour, minute=0, second=0, microsecond=0)

    # 02시 이전이면 전날 23시 발표가 최신이다.
    previous = available - timedelta(days=1)
    return previous.replace(hour=_VILAGE_HOURS[-1], minute=0, second=0, microsecond=0)
```

- [ ] **Step 4: 통과를 확인한다**

Run: `uv run pytest tests/test_clock.py -v`
Expected: PASS (22건)

- [ ] **Step 5: 게이트를 돌리고 커밋한다**

```bash
uv run ruff format && uv run ruff check && uv run ty check && uv run pytest
git add src/pykma/clock.py tests/test_clock.py
git commit -m "feat: 오퍼레이션별 발표 시각 계산"
```

---

## Task 4: `codes.py` — 카테고리 코드와 강수 문자열

`SKY`/`PTY` 숫자 코드를 뜻으로 옮기고, 강수량이 숫자가 아니라 문자열로 오는
고통 #5를 흡수한다. **오퍼레이션마다 기온·강수 코드가 다른 것**(고통 #4)도 여기서 흡수한다.

**Files:**
- Create: `src/pykma/codes.py`, `tests/test_codes.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `Sky` — `IntEnum`. `CLEAR = 1`, `PARTLY_CLOUDY = 3`, `CLOUDY = 4`
  - `Precipitation` — `IntEnum`. `NONE = 0`, `RAIN = 1`, `RAIN_SNOW = 2`, `SNOW = 3`,
    `SHOWER = 4`, `DRIZZLE = 5`, `DRIZZLE_SNOW = 6`, `SNOW_FLURRY = 7`
  - `Amount` — `@dataclass(frozen=True)`, 필드 `raw: str`, `value: float | None`
  - `parse_amount(raw: str) -> Amount`
  - `parse_sky(raw: str) -> Sky | None`
  - `parse_precipitation(raw: str) -> Precipitation | None`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_codes.py`:

```python
import pytest

from pykma.codes import Amount, Precipitation, Sky, parse_amount, parse_precipitation, parse_sky


@pytest.mark.parametrize(
    ("raw", "value"),
    [
        # 없음을 뜻하는 문자열들 — 0.0이지 None이 아니다.
        ("강수없음", 0.0),
        ("적설없음", 0.0),
        ("-", 0.0),
        # 하한을 뜻하는 문자열 — 범주의 아래끝을 값으로 삼는다.
        ("1.0mm 미만", 0.0),
        ("1mm 미만", 0.0),
        ("1.0cm 미만", 0.0),
        # 범위 — 아래끝을 값으로 삼는다.
        ("30.0~50.0mm", 30.0),
        ("1.0~4.9cm", 1.0),
        # 이상 — 그 값을 그대로 쓴다.
        ("50.0mm 이상", 50.0),
        ("5.0cm 이상", 5.0),
        # 평범한 숫자
        ("10.0mm", 10.0),
        ("3.5", 3.5),
    ],
)
def test_강수_문자열을_숫자로_읽는다(raw, value):
    # given / when
    amount = parse_amount(raw)

    # then
    assert amount.value == value
    assert amount.raw == raw


def test_모르는_형식은_값을_비우되_원문은_남긴다():
    # given — 기상청이 새 범주 문구를 내놓아도 데이터를 삼키면 안 된다
    raw = "폭우 예상"

    # when
    amount = parse_amount(raw)

    # then
    assert amount.value is None
    assert amount.raw == raw


def test_강수량은_불변이다():
    # given
    amount = parse_amount("10.0mm")

    # when / then
    with pytest.raises(AttributeError):
        amount.value = 1.0  # type: ignore[misc]


@pytest.mark.parametrize(
    ("raw", "expected"),
    [("1", Sky.CLEAR), ("3", Sky.PARTLY_CLOUDY), ("4", Sky.CLOUDY)],
)
def test_하늘상태_코드를_읽는다(raw, expected):
    # given / when / then
    assert parse_sky(raw) == expected


@pytest.mark.parametrize("raw", ["2", "9", "", "맑음"])
def test_모르는_하늘상태는_비운다(raw):
    # given / when / then — SKY에 2는 없다(결번)
    assert parse_sky(raw) is None


@pytest.mark.parametrize(
    ("raw", "expected"),
    [
        ("0", Precipitation.NONE),
        ("1", Precipitation.RAIN),
        ("2", Precipitation.RAIN_SNOW),
        ("3", Precipitation.SNOW),
        ("4", Precipitation.SHOWER),
        ("5", Precipitation.DRIZZLE),
        ("6", Precipitation.DRIZZLE_SNOW),
        ("7", Precipitation.SNOW_FLURRY),
    ],
)
def test_강수형태_코드를_읽는다(raw, expected):
    # given / when / then
    assert parse_precipitation(raw) == expected


@pytest.mark.parametrize("raw", ["8", "", "비"])
def test_모르는_강수형태는_비운다(raw):
    # given / when / then
    assert parse_precipitation(raw) is None


def test_없음은_0이고_모름은_None이라_서로_구별된다():
    # given / when
    none = parse_amount("강수없음")
    unknown = parse_amount("???")

    # then — 이 둘을 섞으면 "비가 안 온다"와 "모른다"가 같아진다
    assert none.value == 0.0
    assert unknown.value is None
    assert Amount(raw="강수없음", value=0.0) == none
```

- [ ] **Step 2: 실패를 확인한다**

Run: `uv run pytest tests/test_codes.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'pykma.codes'`

- [ ] **Step 3: 구현한다**

`src/pykma/codes.py`:

```python
"""응답의 코드와 값을 뜻으로 옮긴다.

기상청 응답에는 두 종류의 함정이 있다.

첫째, 강수량(PCP)·신적설(SNO)·1시간강수량(RN1)이 숫자가 아니라 범주 문자열로
온다. "강수없음", "1.0mm 미만", "30.0~50.0mm", "50.0mm 이상" 같은 것들이다.
그대로 float()에 넣으면 터진다.

둘째, 같은 뜻인데 오퍼레이션마다 코드가 다르다. 기온은 단기예보에서 TMP,
초단기에서 T1H다. 강수량은 PCP와 RN1이다. models.py가 이 대응을 쓴다.

모르는 코드와 모르는 문자열은 버리지 않는다. 기상청이 범주를 추가해도
라이브러리가 데이터를 삼키면 안 된다.
"""

import re
from dataclasses import dataclass
from enum import IntEnum

__all__ = [
    "Amount",
    "Precipitation",
    "Sky",
    "parse_amount",
    "parse_precipitation",
    "parse_sky",
]


class Sky(IntEnum):
    """하늘상태(SKY). 2는 결번이다."""

    CLEAR = 1
    PARTLY_CLOUDY = 3
    CLOUDY = 4


class Precipitation(IntEnum):
    """강수형태(PTY).

    4~7은 오퍼레이션에 따라 나오는 범위가 다르다. 단기예보는 0~4를,
    초단기는 0~3과 5~7을 쓴다. 라이브러리는 둘 다 받는다.
    """

    NONE = 0
    RAIN = 1
    RAIN_SNOW = 2
    SNOW = 3
    SHOWER = 4
    DRIZZLE = 5
    DRIZZLE_SNOW = 6
    SNOW_FLURRY = 7


@dataclass(frozen=True)
class Amount:
    """강수량 또는 적설량.

    value의 단위는 원본 카테고리를 따른다 — PCP·RN1은 mm, SNO는 cm다.
    범주 문자열이면 **범주의 아래끝**이 value가 된다. 읽지 못한 형식은
    value가 None이고, 그때도 raw는 원문 그대로 남는다.
    """

    raw: str
    value: float | None


# "없음"을 뜻하는 표기들. 0.0이지 None이 아니다 —
# "비가 안 온다"와 "모른다"를 섞으면 안 된다.
_NOTHING = frozenset({"강수없음", "적설없음", "-", ""})

# "1.0mm 미만", "1mm 미만", "1.0cm 미만"
_BELOW = re.compile(r"^\d+(?:\.\d+)?\s*(?:mm|cm)?\s*미만$")

# "30.0~50.0mm", "1.0~4.9cm" — 아래끝을 취한다.
_RANGE = re.compile(r"^(\d+(?:\.\d+)?)\s*~\s*\d+(?:\.\d+)?\s*(?:mm|cm)?$")

# "50.0mm 이상", "5.0cm 이상", "10.0mm", "3.5"
_SINGLE = re.compile(r"^(\d+(?:\.\d+)?)\s*(?:mm|cm)?(?:\s*이상)?$")


def parse_amount(raw: str) -> Amount:
    """강수·적설 범주 문자열을 숫자로 읽는다. 못 읽으면 value가 None이다."""
    text = raw.strip()

    if text in _NOTHING:
        return Amount(raw=raw, value=0.0)

    # "1.0mm 미만"은 하한이 0이다. 0.0~1.0 구간의 아래끝.
    if _BELOW.match(text):
        return Amount(raw=raw, value=0.0)

    if match := _RANGE.match(text):
        return Amount(raw=raw, value=float(match.group(1)))

    if match := _SINGLE.match(text):
        return Amount(raw=raw, value=float(match.group(1)))

    return Amount(raw=raw, value=None)


def parse_sky(raw: str) -> Sky | None:
    """하늘상태 코드. 모르는 값이면 None."""
    try:
        return Sky(int(raw))
    except (ValueError, TypeError):
        return None


def parse_precipitation(raw: str) -> Precipitation | None:
    """강수형태 코드. 모르는 값이면 None."""
    try:
        return Precipitation(int(raw))
    except (ValueError, TypeError):
        return None
```

- [ ] **Step 4: 통과를 확인한다**

Run: `uv run pytest tests/test_codes.py -v`
Expected: PASS (28건)

- [ ] **Step 5: 게이트를 돌리고 커밋한다**

```bash
uv run ruff format && uv run ruff check && uv run ty check && uv run pytest
git add src/pykma/codes.py tests/test_codes.py
git commit -m "feat: 카테고리 코드와 강수 범주 문자열 해석"
```

---

## Task 5: `models.py` — 평평한 행을 시간대로 묶기

응답이 `fcstDate/fcstTime/category/fcstValue` 한 줄씩 수백 개로 온다. 쓰려면
시간대별로 pivot해야 하고, 그걸 각자 하고 있는 것이 고통 #3이다.

**Files:**
- Create: `src/pykma/models.py`, `tests/test_models.py`

**Interfaces:**
- Consumes: `pykma.codes` (`Amount`, `Precipitation`, `Sky`, `parse_amount`, `parse_precipitation`, `parse_sky`)
- Produces:
  - `Observation` — `@dataclass(frozen=True)`. 필드: `observed_at: datetime`,
    `temperature: float | None`, `humidity: int | None`, `precipitation: Amount | None`,
    `precipitation_type: Precipitation | None`, `wind_speed: float | None`,
    `wind_direction: int | None`, `raw: dict[str, str]`
  - `Forecast` — `@dataclass(frozen=True)`. 필드: `at: datetime`,
    `temperature: float | None`, `sky: Sky | None`, `precipitation_type: Precipitation | None`,
    `rain_probability: int | None`, `precipitation: Amount | None`, `snow: Amount | None`,
    `humidity: int | None`, `wind_speed: float | None`, `wind_direction: int | None`,
    `min_temperature: float | None`, `max_temperature: float | None`, `raw: dict[str, str]`
  - `build_observation(items: list[dict[str, str]]) -> Observation`
  - `build_forecasts(items: list[dict[str, str]]) -> list[Forecast]`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_models.py`:

```python
from datetime import datetime

from pykma.codes import Precipitation, Sky
from pykma.models import build_forecasts, build_observation


def _ncst(category: str, value: str) -> dict[str, str]:
    return {
        "baseDate": "20260907",
        "baseTime": "1400",
        "category": category,
        "obsrValue": value,
        "nx": "60",
        "ny": "127",
    }


def _fcst(time: str, category: str, value: str) -> dict[str, str]:
    return {
        "baseDate": "20260907",
        "baseTime": "1400",
        "fcstDate": "20260907",
        "fcstTime": time,
        "category": category,
        "fcstValue": value,
        "nx": "60",
        "ny": "127",
    }


def test_실황은_한_개의_관측으로_묶인다():
    # given — 초단기실황은 T1H(기온)와 RN1(강수량)을 쓴다
    items = [
        _ncst("T1H", "23.4"),
        _ncst("REH", "65"),
        _ncst("RN1", "강수없음"),
        _ncst("PTY", "0"),
        _ncst("WSD", "2.1"),
        _ncst("VEC", "270"),
    ]

    # when
    observation = build_observation(items)

    # then
    assert observation.observed_at == datetime(2026, 9, 7, 14, 0)
    assert observation.temperature == 23.4
    assert observation.humidity == 65
    assert observation.precipitation is not None
    assert observation.precipitation.value == 0.0
    assert observation.precipitation_type is Precipitation.NONE
    assert observation.wind_speed == 2.1
    assert observation.wind_direction == 270


def test_예보는_시간대별로_한_개씩_묶인다():
    # given — 두 시간대가 뒤섞여 들어온다
    items = [
        _fcst("1500", "TMP", "24"),
        _fcst("1600", "TMP", "23"),
        _fcst("1500", "SKY", "1"),
        _fcst("1600", "SKY", "4"),
        _fcst("1500", "POP", "20"),
        _fcst("1600", "POP", "60"),
    ]

    # when
    forecasts = build_forecasts(items)

    # then
    assert len(forecasts) == 2
    assert forecasts[0].at == datetime(2026, 9, 7, 15, 0)
    assert forecasts[0].temperature == 24.0
    assert forecasts[0].sky is Sky.CLEAR
    assert forecasts[0].rain_probability == 20
    assert forecasts[1].at == datetime(2026, 9, 7, 16, 0)
    assert forecasts[1].sky is Sky.CLOUDY
    assert forecasts[1].rain_probability == 60


def test_예보는_시간_순으로_정렬된다():
    # given — 입력 순서가 뒤집혀 있다
    items = [_fcst("1600", "TMP", "23"), _fcst("1500", "TMP", "24")]

    # when
    forecasts = build_forecasts(items)

    # then
    assert [f.at.hour for f in forecasts] == [15, 16]


def test_초단기예보의_T1H도_기온으로_읽힌다():
    # given — 단기예보는 TMP, 초단기예보는 T1H. 같은 속성으로 나와야 한다
    items = [_fcst("1500", "T1H", "24.5")]

    # when
    forecasts = build_forecasts(items)

    # then
    assert forecasts[0].temperature == 24.5


def test_초단기예보의_RN1도_강수량으로_읽힌다():
    # given — 단기예보는 PCP, 초단기예보는 RN1
    items = [_fcst("1500", "RN1", "1.0mm 미만")]

    # when
    forecasts = build_forecasts(items)

    # then
    assert forecasts[0].precipitation is not None
    assert forecasts[0].precipitation.value == 0.0
    assert forecasts[0].precipitation.raw == "1.0mm 미만"


def test_일_최저최고기온을_읽는다():
    # given — TMN/TMX는 하루에 한 번만 실려 온다
    items = [_fcst("0600", "TMN", "18.0"), _fcst("1500", "TMX", "27.0")]

    # when
    forecasts = build_forecasts(items)

    # then
    assert forecasts[0].min_temperature == 18.0
    assert forecasts[1].max_temperature == 27.0


def test_모르는_카테고리도_raw에_남는다():
    # given — 기상청이 카테고리를 추가해도 삼키면 안 된다
    items = [_fcst("1500", "TMP", "24"), _fcst("1500", "XYZ", "unknown")]

    # when
    forecasts = build_forecasts(items)

    # then
    assert forecasts[0].raw["XYZ"] == "unknown"
    assert forecasts[0].raw["TMP"] == "24"


def test_숫자로_못_읽는_값은_그_필드만_비우고_나머지는_살린다():
    # given — TMP가 깨졌지만 SKY는 멀쩡하다
    items = [_fcst("1500", "TMP", "n/a"), _fcst("1500", "SKY", "1")]

    # when
    forecasts = build_forecasts(items)

    # then
    assert forecasts[0].temperature is None
    assert forecasts[0].sky is Sky.CLEAR
    assert forecasts[0].raw["TMP"] == "n/a"


def test_빈_목록은_빈_결과다():
    # given / when / then
    assert build_forecasts([]) == []
```

- [ ] **Step 2: 실패를 확인한다**

Run: `uv run pytest tests/test_models.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'pykma.models'`

- [ ] **Step 3: 구현한다**

`src/pykma/models.py`:

```python
"""응답의 평평한 행을 시간대별 객체로 묶는다.

기상청 응답은 한 시간대의 값들이 한 행에 모여 있지 않다. category 하나가
한 행이라 6시간치 예보가 수십 행으로 온다. 쓰려면 (fcstDate, fcstTime)으로
묶어야 하는데, 그 pivot을 다들 각자 다시 짜고 있다.

같은 뜻인데 오퍼레이션마다 다른 코드도 여기서 흡수한다. 기온은 단기예보에서
TMP, 초단기에서 T1H다. 강수량은 PCP와 RN1이다. 사용자는 .temperature 하나만
보면 된다.

모르는 카테고리도 raw에 남긴다. 값 하나가 깨져도 그 필드만 비우고 나머지
시간대는 살린다 — 한 칸 때문에 예보 전체를 잃으면 안 된다.
"""

from dataclasses import dataclass
from datetime import datetime

from .codes import Amount, Precipitation, Sky, parse_amount, parse_precipitation, parse_sky

__all__ = ["Forecast", "Observation", "build_forecasts", "build_observation"]


@dataclass(frozen=True)
class Observation:
    """초단기실황 한 개. 예보가 아니라 실제 관측값이다."""

    observed_at: datetime
    temperature: float | None
    humidity: int | None
    precipitation: Amount | None
    precipitation_type: Precipitation | None
    wind_speed: float | None
    wind_direction: int | None
    raw: dict[str, str]
    """파싱 전 카테고리 → 값. 모르는 카테고리도 여기 남는다."""


@dataclass(frozen=True)
class Forecast:
    """예보 한 시간대. 초단기예보와 단기예보가 같은 타입을 쓴다."""

    at: datetime
    temperature: float | None
    sky: Sky | None
    precipitation_type: Precipitation | None
    rain_probability: int | None
    precipitation: Amount | None
    snow: Amount | None
    humidity: int | None
    wind_speed: float | None
    wind_direction: int | None
    min_temperature: float | None
    max_temperature: float | None
    raw: dict[str, str]
    """파싱 전 카테고리 → 값. 모르는 카테고리도 여기 남는다."""


def _to_float(raw: dict[str, str], *categories: str) -> float | None:
    """주어진 카테고리 중 먼저 있는 것을 실수로 읽는다. 못 읽으면 None."""
    for category in categories:
        if category in raw:
            try:
                return float(raw[category])
            except ValueError:
                return None
    return None


def _to_int(raw: dict[str, str], *categories: str) -> int | None:
    """주어진 카테고리 중 먼저 있는 것을 정수로 읽는다. 못 읽으면 None."""
    value = _to_float(raw, *categories)
    return None if value is None else int(value)


def _to_amount(raw: dict[str, str], *categories: str) -> Amount | None:
    """주어진 카테고리 중 먼저 있는 것을 강수량으로 읽는다."""
    for category in categories:
        if category in raw:
            return parse_amount(raw[category])
    return None


def _to_sky(raw: dict[str, str]) -> Sky | None:
    return parse_sky(raw["SKY"]) if "SKY" in raw else None


def _to_precipitation_type(raw: dict[str, str]) -> Precipitation | None:
    return parse_precipitation(raw["PTY"]) if "PTY" in raw else None


def _parse_when(date: str, time: str) -> datetime:
    """'20260907' + '1400' → datetime."""
    return datetime.strptime(f"{date}{time}", "%Y%m%d%H%M")


def build_observation(items: list[dict[str, str]]) -> Observation:
    """초단기실황 응답의 행들을 관측 하나로 묶는다."""
    raw = {item["category"]: item["obsrValue"] for item in items}
    first = items[0]

    return Observation(
        observed_at=_parse_when(first["baseDate"], first["baseTime"]),
        # 실황은 T1H만 쓰지만, 대응표를 한 곳에 모아 두기 위해 TMP도 받아 둔다.
        temperature=_to_float(raw, "T1H", "TMP"),
        humidity=_to_int(raw, "REH"),
        precipitation=_to_amount(raw, "RN1", "PCP"),
        precipitation_type=_to_precipitation_type(raw),
        wind_speed=_to_float(raw, "WSD"),
        wind_direction=_to_int(raw, "VEC"),
        raw=raw,
    )


def build_forecasts(items: list[dict[str, str]]) -> list[Forecast]:
    """예보 응답의 행들을 시간대별로 묶어 시간 순으로 돌려준다."""
    grouped: dict[tuple[str, str], dict[str, str]] = {}
    for item in items:
        key = (item["fcstDate"], item["fcstTime"])
        grouped.setdefault(key, {})[item["category"]] = item["fcstValue"]

    forecasts = [
        Forecast(
            at=_parse_when(date, time),
            # 단기예보는 TMP, 초단기예보는 T1H. 사용자에겐 같은 속성이다.
            temperature=_to_float(raw, "TMP", "T1H"),
            sky=_to_code(raw, "SKY", parse_sky),
            precipitation_type=_to_code(raw, "PTY", parse_precipitation),
            rain_probability=_to_int(raw, "POP"),
            # 단기예보는 PCP, 초단기예보는 RN1.
            precipitation=_to_amount(raw, "PCP", "RN1"),
            snow=_to_amount(raw, "SNO"),
            humidity=_to_int(raw, "REH"),
            wind_speed=_to_float(raw, "WSD"),
            wind_direction=_to_int(raw, "VEC"),
            min_temperature=_to_float(raw, "TMN"),
            max_temperature=_to_float(raw, "TMX"),
            raw=raw,
        )
        for (date, time), raw in grouped.items()
    ]

    return sorted(forecasts, key=lambda forecast: forecast.at)
```

- [ ] **Step 4: 통과를 확인한다**

Run: `uv run pytest tests/test_models.py -v`
Expected: PASS (9건)

`_to_code`의 제네릭 문법(`def _to_code[T]`)은 Python 3.12+ 전용이다.
3.11에서 `SyntaxError`가 나면 제네릭을 떼고 `-> Sky | Precipitation | None`으로 바꾼다.
CI 매트릭스가 3.11을 포함하므로 이 문제는 Step 5에서 잡힌다.

- [ ] **Step 5: 게이트를 돌리고 커밋한다**

```bash
uv run ruff format && uv run ruff check && uv run ty check && uv run pytest
git add src/pykma/models.py tests/test_models.py
git commit -m "feat: 평평한 응답 행을 시간대별 객체로"
```

---

## Task 6: `errors.py` + `client.py` — HTTP와 가짜 200

포털은 인증에 실패해도 HTTP 200을 준다. 본문만 XML 에러다. 그리고 인코딩된
인증키를 그대로 넘기면 `%`가 재인코딩돼 실패한다. 고통 #6이다.

**Files:**
- Create: `src/pykma/errors.py`, `src/pykma/client.py`,
  `tests/test_errors.py`, `tests/test_client.py`,
  `tests/fixtures/vilage_fcst.json`, `tests/fixtures/auth_error.xml`
- Modify: `src/pykma/__init__.py`

**Interfaces:**
- Consumes: `pykma.clock` (`Operation`, `base_for`), `pykma.grid` (`to_grid`),
  `pykma.models` (`Forecast`, `Observation`, `build_forecasts`, `build_observation`)
- Produces:
  - `KMAError(Exception)` — 뿌리
  - `AuthError(KMAError)`, `QuotaError(KMAError)`, `RequestError(KMAError)`, `ResponseError(KMAError)`
  - `raise_for_result(code: str, message: str) -> None`
  - `KMA` — 생성자 `KMA(service_key: str | None = None, *, timeout: float = 10.0)`.
    메서드 `now`/`soon`/`forecast` 와 비동기 `anow`/`asoon`/`aforecast`.
    전부 `(lat: float, lon: float, *, at: datetime | None = None)`.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_errors.py`:

```python
import pytest

from pykma.errors import AuthError, KMAError, QuotaError, RequestError, raise_for_result


def test_정상은_아무것도_던지지_않는다():
    # given / when / then
    raise_for_result("00", "NORMAL_SERVICE")


@pytest.mark.parametrize("code", ["20", "30", "31", "32", "33"])
def test_인증_관련_코드는_AuthError다(code):
    # given / when / then
    with pytest.raises(AuthError):
        raise_for_result(code, "SERVICE_ACCESS_DENIED_ERROR")


def test_한도_초과는_QuotaError다():
    # given / when / then
    with pytest.raises(QuotaError):
        raise_for_result("22", "LIMITED_NUMBER_OF_SERVICE_REQUESTS_EXCEEDS_ERROR")


@pytest.mark.parametrize("code", ["10", "11", "12"])
def test_파라미터_오류는_RequestError다(code):
    # given / when / then
    with pytest.raises(RequestError):
        raise_for_result(code, "INVALID_REQUEST_PARAMETER_ERROR")


def test_모르는_코드도_KMAError로_올라온다():
    # given / when / then — 조용히 넘기지 않는다
    with pytest.raises(KMAError):
        raise_for_result("99", "UNKNOWN_ERROR")


def test_예외_메시지에_코드와_사유가_담긴다():
    # given / when
    with pytest.raises(AuthError) as caught:
        raise_for_result("30", "SERVICE_KEY_IS_NOT_REGISTERED_ERROR")

    # then
    assert "30" in str(caught.value)
    assert "SERVICE_KEY_IS_NOT_REGISTERED_ERROR" in str(caught.value)
```

`tests/test_client.py`:

```python
import json
from datetime import datetime
from pathlib import Path

import httpx
import pytest

from pykma import KMA
from pykma.errors import AuthError, ResponseError

FIXTURES = Path(__file__).parent / "fixtures"
AT = datetime(2026, 9, 7, 14, 30)


def _transport(handler):
    return httpx.MockTransport(handler)


def _ok(request: httpx.Request) -> httpx.Response:
    body = json.loads((FIXTURES / "vilage_fcst.json").read_text(encoding="utf-8"))
    return httpx.Response(200, json=body)


def test_예보를_시간대_객체로_돌려준다():
    # given
    kma = KMA(service_key="test-key", transport=_transport(_ok))

    # when
    forecasts = kma.forecast(37.5665, 126.9780, at=AT)

    # then
    assert forecasts
    assert forecasts[0].at < forecasts[-1].at


def test_위경도를_격자로_바꿔_보낸다():
    # given
    seen: dict[str, str] = {}

    def handler(request: httpx.Request) -> httpx.Response:
        seen.update(dict(request.url.params))
        return _ok(request)

    kma = KMA(service_key="test-key", transport=_transport(handler))

    # when
    kma.forecast(37.5665, 126.9780, at=AT)

    # then — 서울시청은 (60, 127)이다
    assert seen["nx"] == "60"
    assert seen["ny"] == "127"


def test_발표시각을_계산해_보낸다():
    # given
    seen: dict[str, str] = {}

    def handler(request: httpx.Request) -> httpx.Response:
        seen.update(dict(request.url.params))
        return _ok(request)

    kma = KMA(service_key="test-key", transport=_transport(handler))

    # when — 14:30이면 14시 발표(+10분 제공)가 최신이다
    kma.forecast(37.5665, 126.9780, at=AT)

    # then
    assert seen["base_date"] == "20260907"
    assert seen["base_time"] == "1400"


def test_인코딩된_인증키를_디코딩해_보낸다():
    # given — 포털은 인코딩·디코딩 두 형태의 키를 준다.
    # 인코딩된 쪽을 그대로 넘기면 %가 재인코딩돼 인증에 실패한다.
    seen: dict[str, str] = {}

    def handler(request: httpx.Request) -> httpx.Response:
        seen.update(dict(request.url.params))
        return _ok(request)

    kma = KMA(service_key="abc%2Bdef%3D%3D", transport=_transport(handler))

    # when
    kma.forecast(37.5665, 126.9780, at=AT)

    # then
    assert seen["serviceKey"] == "abc+def=="


def test_HTTP_200인데_XML_에러면_예외를_던진다():
    # given — 인증 실패 시 포털은 200과 함께 XML 에러 본문을 준다
    xml = (FIXTURES / "auth_error.xml").read_text(encoding="utf-8")

    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(200, text=xml, headers={"content-type": "application/xml"})

    kma = KMA(service_key="wrong", transport=_transport(handler))

    # when / then
    with pytest.raises(AuthError):
        kma.forecast(37.5665, 126.9780, at=AT)


def test_본문_결과코드가_오류면_예외를_던진다():
    # given — JSON 봉투 안에 오류 코드가 담겨 오는 경우
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(
            200,
            json={"response": {"header": {"resultCode": "22", "resultMsg": "LIMITED"}}},
        )

    kma = KMA(service_key="test-key", transport=_transport(handler))

    # when / then
    from pykma.errors import QuotaError

    with pytest.raises(QuotaError):
        kma.forecast(37.5665, 126.9780, at=AT)


def test_예외_메시지에_인증키가_들어가지_않는다():
    # given — 로그와 스택트레이스로 키가 새는 가장 흔한 경로다
    secret = "super-secret-key-value"

    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(200, text="not json at all")

    kma = KMA(service_key=secret, transport=_transport(handler))

    # when
    with pytest.raises(ResponseError) as caught:
        kma.forecast(37.5665, 126.9780, at=AT)

    # then
    assert secret not in str(caught.value)
    assert secret not in repr(caught.value)


def test_인증키가_없으면_환경변수를_읽는다(monkeypatch):
    # given
    monkeypatch.setenv("KMA_SERVICE_KEY", "from-env")
    seen: dict[str, str] = {}

    def handler(request: httpx.Request) -> httpx.Response:
        seen.update(dict(request.url.params))
        return _ok(request)

    kma = KMA(transport=_transport(handler))

    # when
    kma.forecast(37.5665, 126.9780, at=AT)

    # then
    assert seen["serviceKey"] == "from-env"


def test_인증키가_아무_데도_없으면_바로_거절한다(monkeypatch):
    # given
    monkeypatch.delenv("KMA_SERVICE_KEY", raising=False)

    # when / then — 네트워크를 치기 전에 막는다
    with pytest.raises(AuthError):
        KMA().forecast(37.5665, 126.9780, at=AT)


async def test_비동기도_같은_결과를_준다():
    # given
    kma = KMA(service_key="test-key", transport=_transport(_ok))

    # when
    forecasts = await kma.aforecast(37.5665, 126.9780, at=AT)

    # then
    assert forecasts
    assert forecasts[0].at < forecasts[-1].at
```

- [ ] **Step 2: 픽스처를 만든다**

`tests/fixtures/auth_error.xml` — 포털이 인증 실패 시 주는 실제 형태:

```xml
<OpenAPI_ServiceResponse>
	<cmmMsgHeader>
		<errMsg>SERVICE ERROR</errMsg>
		<returnAuthMsg>SERVICE_KEY_IS_NOT_REGISTERED_ERROR</returnAuthMsg>
		<returnReasonCode>30</returnReasonCode>
	</cmmMsgHeader>
</OpenAPI_ServiceResponse>
```

`tests/fixtures/vilage_fcst.json` — 정상 응답 봉투. **실제 호출로 녹화한 것을 쓴다.**
인증키를 받은 뒤 아래를 한 번 돌려 저장한다:

```bash
uv run python - <<'PY'
import json, os, urllib.parse, urllib.request

key = os.environ["KMA_SERVICE_KEY"]
params = urllib.parse.urlencode({
    "serviceKey": key, "pageNo": 1, "numOfRows": 300, "dataType": "JSON",
    "base_date": "20260907", "base_time": "1400", "nx": 60, "ny": 127,
})
url = ("http://apis.data.go.kr/1360000/VilageFcstInfoService_2.0"
       f"/getVilageFcst?{params}")
body = json.load(urllib.request.urlopen(url))
with open("tests/fixtures/vilage_fcst.json", "w", encoding="utf-8") as f:
    json.dump(body, f, ensure_ascii=False, indent=2)
print(body["response"]["header"])
PY
```

저장 후 **파일에 인증키가 없는지 확인한다**: `grep -c "$KMA_SERVICE_KEY" tests/fixtures/vilage_fcst.json` 이 `0`이어야 한다.

- [ ] **Step 3: 실패를 확인한다**

Run: `uv run pytest tests/test_errors.py tests/test_client.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'pykma.errors'`

- [ ] **Step 4: `errors.py`를 구현한다**

```python
"""예외 계층과 포털 결과코드 매핑.

공공데이터포털은 실패해도 HTTP 200을 준다. 본문의 resultCode(또는 인증 실패
시 XML의 returnReasonCode)를 읽어야 실패를 안다. 그 코드를 뜻이 있는 예외로
올리는 것이 이 모듈의 일이다.

예외 메시지에 인증키를 절대 넣지 않는다. 로그와 스택트레이스로 키가 새는
가장 흔한 경로다.
"""

__all__ = [
    "AuthError",
    "KMAError",
    "QuotaError",
    "RequestError",
    "ResponseError",
    "raise_for_result",
]


class KMAError(Exception):
    """pykma가 던지는 모든 예외의 뿌리."""


class AuthError(KMAError):
    """인증키가 없거나, 등록되지 않았거나, 권한이 없다."""


class QuotaError(KMAError):
    """요청 한도를 넘었다."""


class RequestError(KMAError):
    """파라미터가 잘못됐거나 서비스가 없다."""


class ResponseError(KMAError):
    """응답을 이해하지 못했다."""


_SUCCESS = "00"

_AUTH_CODES = frozenset({"20", "21", "30", "31", "32", "33"})
_QUOTA_CODES = frozenset({"22"})
_REQUEST_CODES = frozenset({"10", "11", "12"})


def raise_for_result(code: str, message: str) -> None:
    """포털 결과코드를 예외로 올린다. 정상이면 아무것도 하지 않는다."""
    if code == _SUCCESS:
        return

    detail = f"기상청 API 오류 [{code}] {message}"

    if code in _AUTH_CODES:
        raise AuthError(detail)
    if code in _QUOTA_CODES:
        raise QuotaError(detail)
    if code in _REQUEST_CODES:
        raise RequestError(detail)

    # 03(NODATA)을 포함해 모르는 코드도 조용히 넘기지 않는다.
    raise KMAError(detail)
```

- [ ] **Step 5: `client.py`를 구현한다**

```python
"""기상청 단기예보 조회서비스 호출.

이 파일만 네트워크를 안다. 나머지 모듈은 전부 순수 함수라 네트워크 없이
테스트된다.

여기서 흡수하는 함정 둘.

첫째, 포털이 인증키를 인코딩·디코딩 두 형태로 준다. 인코딩된 쪽을 그대로
쿼리에 넣으면 %가 다시 인코딩돼(%2B → %252B) 인증에 실패한다. 키에 %가
보이면 한 번 디코딩해서 보낸다.

둘째, 실패해도 HTTP는 200이다. 인증이 틀리면 JSON이 아니라 XML 에러가 오고,
그 외의 실패는 정상 봉투 안의 resultCode로 온다. 둘 다 예외로 올린다.
"""

import os
import re
from datetime import datetime
from typing import Any
from urllib.parse import unquote

import httpx

from .clock import Operation, base_for
from .errors import AuthError, ResponseError, raise_for_result
from .grid import to_grid
from .models import Forecast, Observation, build_forecasts, build_observation

__all__ = ["KMA"]

_BASE_URL = "http://apis.data.go.kr/1360000/VilageFcstInfoService_2.0"
_ENV_KEY = "KMA_SERVICE_KEY"

# 한 번에 받을 행 수. 단기예보 3일치가 대략 800행이라 넉넉히 잡는다.
_ROWS = {
    Operation.ULTRA_NCST: 100,
    Operation.ULTRA_FCST: 100,
    Operation.VILAGE_FCST: 1000,
}

# 인증 실패 시 오는 XML에서 코드와 사유를 뽑는다. 이 응답만을 위해
# XML 파서를 끌어들일 이유가 없다.
_XML_CODE = re.compile(r"<returnReasonCode>(\d+)</returnReasonCode>")
_XML_MSG = re.compile(r"<returnAuthMsg>([^<]*)</returnAuthMsg>")


class KMA:
    """기상청 단기예보 API 클라이언트."""

    def __init__(
        self,
        service_key: str | None = None,
        *,
        timeout: float = 10.0,
        transport: httpx.BaseTransport | httpx.AsyncBaseTransport | None = None,
    ) -> None:
        """인증키를 받는다. 없으면 환경변수 KMA_SERVICE_KEY를 읽는다.

        transport는 테스트에서 httpx.MockTransport를 끼우기 위한 것이다.
        """
        self._key = service_key or os.environ.get(_ENV_KEY) or ""
        self._timeout = timeout
        self._transport = transport

    def now(self, lat: float, lon: float, *, at: datetime | None = None) -> Observation:
        """지금의 초단기실황."""
        items = self._get(Operation.ULTRA_NCST, lat, lon, at)
        return build_observation(items)

    def soon(self, lat: float, lon: float, *, at: datetime | None = None) -> list[Forecast]:
        """향후 6시간 초단기예보."""
        return build_forecasts(self._get(Operation.ULTRA_FCST, lat, lon, at))

    def forecast(self, lat: float, lon: float, *, at: datetime | None = None) -> list[Forecast]:
        """향후 3일 단기예보."""
        return build_forecasts(self._get(Operation.VILAGE_FCST, lat, lon, at))

    async def anow(self, lat: float, lon: float, *, at: datetime | None = None) -> Observation:
        """`now`의 비동기 판."""
        items = await self._aget(Operation.ULTRA_NCST, lat, lon, at)
        return build_observation(items)

    async def asoon(self, lat: float, lon: float, *, at: datetime | None = None) -> list[Forecast]:
        """`soon`의 비동기 판."""
        return build_forecasts(await self._aget(Operation.ULTRA_FCST, lat, lon, at))

    async def aforecast(
        self, lat: float, lon: float, *, at: datetime | None = None
    ) -> list[Forecast]:
        """`forecast`의 비동기 판."""
        return build_forecasts(await self._aget(Operation.VILAGE_FCST, lat, lon, at))

    def _params(
        self, operation: Operation, lat: float, lon: float, at: datetime | None
    ) -> dict[str, str | int]:
        if not self._key:
            raise AuthError(
                f"인증키가 없다. KMA(service_key=...)로 넘기거나 환경변수 {_ENV_KEY}에 둔다."
            )

        grid = to_grid(lat, lon)
        base = base_for(operation, at or datetime.now())

        return {
            # 포털이 주는 인코딩된 키를 그대로 쓰면 %가 재인코딩된다.
            "serviceKey": unquote(self._key) if "%" in self._key else self._key,
            "pageNo": 1,
            "numOfRows": _ROWS[operation],
            "dataType": "JSON",
            "base_date": base.date,
            "base_time": base.time,
            "nx": grid.nx,
            "ny": grid.ny,
        }

    def _get(
        self, operation: Operation, lat: float, lon: float, at: datetime | None
    ) -> list[dict[str, str]]:
        params = self._params(operation, lat, lon, at)
        with httpx.Client(timeout=self._timeout, transport=self._transport) as client:  # type: ignore[arg-type]
            response = client.get(f"{_BASE_URL}/{operation.value}", params=params)
        return self._unwrap(response)

    async def _aget(
        self, operation: Operation, lat: float, lon: float, at: datetime | None
    ) -> list[dict[str, str]]:
        params = self._params(operation, lat, lon, at)
        async with httpx.AsyncClient(  # type: ignore[arg-type]
            timeout=self._timeout, transport=self._transport
        ) as client:
            response = await client.get(f"{_BASE_URL}/{operation.value}", params=params)
        return self._unwrap(response)

    def _unwrap(self, response: httpx.Response) -> list[dict[str, str]]:
        """봉투를 벗기고 행 목록을 꺼낸다. 실패는 예외로 올린다."""
        response.raise_for_status()
        text = response.text

        # 인증 실패는 JSON이 아니라 XML로 온다. HTTP는 200이다.
        if "<returnReasonCode>" in text:
            code = match.group(1) if (match := _XML_CODE.search(text)) else "99"
            message = match.group(1) if (match := _XML_MSG.search(text)) else "SERVICE ERROR"
            raise_for_result(code, message)

        try:
            body: Any = response.json()
            header = body["response"]["header"]
        except (ValueError, KeyError, TypeError) as error:
            # 본문을 예외에 싣지 않는다. 쿼리가 되울려 오면 인증키가 샌다.
            raise ResponseError("기상청 응답을 이해하지 못했다.") from error

        raise_for_result(str(header["resultCode"]), str(header.get("resultMsg", "")))

        try:
            return list(body["response"]["body"]["items"]["item"])
        except (KeyError, TypeError) as error:
            raise ResponseError("기상청 응답에 자료가 없다.") from error
```

- [ ] **Step 6: `__init__.py`에서 공개 API를 재수출한다**

```python
"""기상청 단기예보 API Python SDK."""

from .client import KMA
from .codes import Amount, Precipitation, Sky
from .errors import AuthError, KMAError, QuotaError, RequestError, ResponseError
from .grid import Grid, to_grid, to_latlon
from .models import Forecast, Observation

__version__ = "0.1.0"

__all__ = [
    "KMA",
    "Amount",
    "AuthError",
    "Forecast",
    "Grid",
    "KMAError",
    "Observation",
    "Precipitation",
    "QuotaError",
    "RequestError",
    "ResponseError",
    "Sky",
    "__version__",
    "to_grid",
    "to_latlon",
]
```

- [ ] **Step 7: 통과를 확인한다**

Run: `uv run pytest -v`
Expected: 전부 PASS. 커버리지 90% 이상.

`httpx.MockTransport`는 동기·비동기 양쪽에서 쓸 수 있다. `transport`는 테스트 전용
주입점이라 `httpx` 타입이 공개 시그니처에 새지만, 대안(자체 프로토콜 정의)이
더 무겁다. 이 하나는 감수한다.

- [ ] **Step 8: 게이트를 돌리고 커밋한다**

```bash
uv run ruff format && uv run ruff check && uv run ty check && uv run pytest
git add src/pykma tests
git commit -m "feat: 클라이언트와 예외 계층"
```

---

## Task 7: README와 PyPI 배포

**완료 기준 5개 중 1·2·3·5번이 여기서 닫힌다.** MCP보다 먼저 배포하는 이유는,
다운로드 숫자가 붙기 시작해야 다음 저장소를 시작할 조건이 충족되기 때문이다.

**Files:**
- Modify: `README.md`
- Create: `README.en.md`, `.github/workflows/publish.yml`, `LICENSE`

**Interfaces:**
- Consumes: Task 6의 공개 API 전부
- Produces: PyPI `pykma` 0.1.0

- [ ] **Step 1: README를 완성한다**

배지, 30초 예제, 인증키 안내, API 표를 넣는다. 검색으로 들어온 사람이
"이게 내 문제를 푸는 물건인가"를 3초 안에 판단할 수 있어야 한다.

````markdown
# pykma

[![PyPI](https://img.shields.io/pypi/v/pykma)](https://pypi.org/project/pykma/)
[![Downloads](https://static.pepy.tech/badge/pykma/month)](https://pepy.tech/project/pykma)
[![CI](https://github.com/alton15/pykma/actions/workflows/ci.yml/badge.svg)](https://github.com/alton15/pykma/actions)

기상청 단기예보 API Python SDK. **위경도만 넣으면 된다.**

격자 변환도, `base_time` 계산도, 카테고리 코드표도 더 안 찾아봐도 된다.

```python
from pykma import KMA

kma = KMA()  # 또는 KMA(service_key="...")

now = kma.now(37.5665, 126.9780)
print(now.temperature, now.humidity)          # 23.4 65

for f in kma.forecast(37.5665, 126.9780):
    print(f.at, f.temperature, f.sky, f.rain_probability)
```

## 이게 없애주는 것

| | 직접 하면 | pykma |
|---|---|---|
| 좌표 | Lambert 변환 함수를 블로그에서 복붙 | `lat, lon` |
| 발표 시각 | 오퍼레이션별 발표 주기 + 제공 지연 + 자정 넘김 처리 | 자동 |
| 응답 | `category`/`fcstValue` 행 800개를 직접 pivot | 시간대별 객체 |
| 코드 | `TMP`? `T1H`? `PTY` 5는 뭐지? | `.temperature`, `.precipitation_type` |
| 강수량 | `"30.0~50.0mm"`를 `float()`에 넣고 터짐 | `.precipitation.value == 30.0` |
| 오류 | HTTP 200인데 XML 에러 | 예외 |

## 설치

```bash
pip install pykma
```

## 인증키

[공공데이터포털 — 단기예보 조회서비스](https://www.data.go.kr/data/15084084/openapi.do)에서
활용신청한다. 승인에 하루 정도 걸린다.

```bash
export KMA_SERVICE_KEY="발급받은 키"
```

인코딩된 키와 디코딩된 키 **어느 쪽을 넣어도 동작한다.**

## API

| 메서드 | 무엇 | 반환 |
|---|---|---|
| `now(lat, lon)` | 초단기실황 — 지금 관측값 | `Observation` |
| `soon(lat, lon)` | 초단기예보 — 향후 6시간 | `list[Forecast]` |
| `forecast(lat, lon)` | 단기예보 — 향후 3일 | `list[Forecast]` |

`anow` / `asoon` / `aforecast` 로 비동기 호출도 된다.

## MCP 서버

Claude 등 에이전트에서 바로 쓸 수 있다.

```json
{
  "mcpServers": {
    "pykma": {
      "command": "uvx",
      "args": ["--from", "pykma[mcp]", "pykma-mcp"],
      "env": { "KMA_SERVICE_KEY": "발급받은 키" }
    }
  }
}
```

## 범위

단기예보 조회서비스(`VilageFcstInfoService_2.0`)만 다룬다. 중기예보, 기상특보,
생활기상지수는 아직 없다.

## 라이선스

MIT
````

**위 「MCP 서버」 절은 Task 8이 끝나야 참이 된다.** Task 8 전에는 이 절을
빼고 배포하고, Task 8 Step 6에서 다시 넣는다. 거짓 설치 안내가 붙은 채로
PyPI에 올라가면 첫 사용자가 첫 시도에서 막힌다.

- [ ] **Step 2: 영문 README를 쓴다**

스펙 §7이 양국어 README를 요구한다. 한국 개발자가 주 사용자지만, PyPI 검색과
GitHub 탐색은 영어로 들어온다.

`README.md` 배지 아래에 한 줄을 넣는다:

```markdown
*[English](README.en.md)*
```

`README.en.md`는 한국어판과 같은 뼈대로 쓴다 — 배지, 30초 예제, 「이게 없애주는 것」
표, 설치, 인증키, API 표, MCP 절, 범위, 라이선스. 도입부는 이렇게 옮긴다:

````markdown
# pykma

Python SDK for the Korea Meteorological Administration (KMA) short-term
forecast API. **Latitude and longitude are all you need.**

No more copy-pasting Lambert projection code, computing `base_time` release
schedules, or looking up category tables.

```python
from pykma import KMA

kma = KMA()  # or KMA(service_key="...")

now = kma.now(37.5665, 126.9780)
print(now.temperature, now.humidity)          # 23.4 65
```

Get a key from the [Korean public data portal](https://www.data.go.kr/data/15084084/openapi.do)
(approval takes about a day), then set `KMA_SERVICE_KEY`.

*[한국어](README.md)*
````

- [ ] **Step 3: LICENSE를 넣는다**

```bash
gh api /licenses/mit --jq '.body' \
  | sed "s/\[year\]/2026/; s/\[fullname\]/alton15/" > LICENSE
```

- [ ] **Step 4: 배포 워크플로를 만든다**

`.github/workflows/publish.yml`:

```yaml
name: publish

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write   # Trusted Publishing에 필요. API 토큰을 두지 않는다.
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv build
      - uses: pypa/gh-action-pypi-publish@release/v1
```

- [ ] **Step 5: PyPI에 Trusted Publisher를 등록한다**

PyPI → Your projects → Publishing → Add a new pending publisher.
- PyPI Project Name: `pykma`
- Owner: `alton15`
- Repository name: `pykma`
- Workflow name: `publish.yml`
- Environment name: `pypi`

**API 토큰을 만들지 않는다.** 저장소 시크릿에 아무것도 넣지 않는다.

- [ ] **Step 6: 배포하고 확인한다**

```bash
uv run ruff format && uv run ruff check && uv run ty check && uv run pytest
git add -A
git commit -m "docs: README와 PyPI 배포 설정"
git push
gh release create v0.1.0 --title "v0.1.0" --notes "첫 배포. 단기예보 3종 + 격자 변환."
gh run watch
```

그리고 **깨끗한 환경에서 실제로 설치해 본다:**

```bash
cd $(mktemp -d)
uv venv && uv pip install pykma
KMA_SERVICE_KEY="..." uv run python -c "
from pykma import KMA
print(KMA().now(37.5665, 126.9780).temperature)
"
```

Expected: 실제 기온이 찍힌다. **이게 완료 기준 1번이다.**

---

## Task 8: MCP 서버

**완료 기준 4번이 여기서 닫힌다.** 이 층이 있어야 만든 사람이 매일 쓴다.

**Files:**
- Create: `src/pykma_mcp/__init__.py`, `src/pykma_mcp/server.py`, `tests/test_mcp.py`
- Modify: `README.md`

**Interfaces:**
- Consumes: `pykma` (`KMA`, `Forecast`, `Observation`)
- Produces: 콘솔 스크립트 `pykma-mcp` → `pykma_mcp.server:main`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_mcp.py`:

```python
from datetime import datetime

from pykma.codes import Amount, Sky
from pykma.models import Forecast, Observation
from pykma_mcp.server import format_forecasts, format_observation


def test_관측을_사람이_읽는_한_줄로_만든다():
    # given
    observation = Observation(
        observed_at=datetime(2026, 9, 7, 14, 0),
        temperature=23.4,
        humidity=65,
        precipitation=Amount(raw="강수없음", value=0.0),
        precipitation_type=None,
        wind_speed=2.1,
        wind_direction=270,
        raw={},
    )

    # when
    text = format_observation(observation)

    # then
    assert "23.4" in text
    assert "65" in text
    assert "2026-09-07 14:00" in text


def test_예보를_시간대별_줄로_만든다():
    # given
    forecasts = [
        Forecast(
            at=datetime(2026, 9, 7, 15, 0),
            temperature=24.0,
            sky=Sky.CLEAR,
            precipitation_type=None,
            rain_probability=20,
            precipitation=None,
            snow=None,
            humidity=None,
            wind_speed=None,
            wind_direction=None,
            min_temperature=None,
            max_temperature=None,
            raw={},
        )
    ]

    # when
    text = format_forecasts(forecasts)

    # then
    assert "15:00" in text
    assert "24.0" in text
    assert "20%" in text


def test_빈_예보도_문장을_준다():
    # given / when
    text = format_forecasts([])

    # then — 빈 문자열을 주면 에이전트가 도구가 실패한 것으로 읽는다
    assert text.strip()
```

- [ ] **Step 2: 실패를 확인한다**

Run: `uv run pytest tests/test_mcp.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'pykma_mcp'`

- [ ] **Step 3: 구현한다**

`src/pykma_mcp/__init__.py`:

```python
"""pykma의 MCP 서버."""
```

`src/pykma_mcp/server.py`:

```python
"""기상청 날씨를 에이전트가 쓸 수 있게 내보내는 MCP 서버.

pykma에만 의존한다. 반대 방향 의존은 없다 — 라이브러리는 MCP를 모른다.

값을 JSON으로 던지지 않고 사람이 읽는 문장으로 만드는 이유: 모델이 읽을
것이라 단위와 시각이 글자로 붙어 있는 편이 오해가 적고 토큰도 적게 든다.
"""

from mcp.server.fastmcp import FastMCP

from pykma import KMA, Forecast, Observation, Precipitation

__all__ = ["format_forecasts", "format_observation", "main"]

mcp = FastMCP("pykma")

_SKY_LABEL = {1: "맑음", 3: "구름많음", 4: "흐림"}
_PTY_LABEL = {
    0: "없음",
    1: "비",
    2: "비/눈",
    3: "눈",
    4: "소나기",
    5: "빗방울",
    6: "빗방울눈날림",
    7: "눈날림",
}


def format_observation(observation: Observation) -> str:
    """관측 하나를 한 줄로."""
    parts = [observation.observed_at.strftime("%Y-%m-%d %H:%M") + " 실황"]

    if observation.temperature is not None:
        parts.append(f"기온 {observation.temperature}도")
    if observation.humidity is not None:
        parts.append(f"습도 {observation.humidity}%")
    if observation.precipitation_type is not None:
        parts.append(f"강수 {_PTY_LABEL.get(observation.precipitation_type, '알수없음')}")
    if observation.precipitation is not None:
        parts.append(f"강수량 {observation.precipitation.raw}")
    if observation.wind_speed is not None:
        parts.append(f"풍속 {observation.wind_speed}m/s")

    return ", ".join(parts)


def format_forecasts(forecasts: list[Forecast]) -> str:
    """예보 목록을 시간대별 한 줄씩."""
    if not forecasts:
        return "해당 시각의 예보 자료가 없다."

    lines = []
    for forecast in forecasts:
        parts = [forecast.at.strftime("%m-%d %H:%M")]
        if forecast.temperature is not None:
            parts.append(f"{forecast.temperature}도")
        if forecast.sky is not None:
            parts.append(_SKY_LABEL.get(forecast.sky, "알수없음"))
        if forecast.precipitation_type not in (None, Precipitation.NONE):
            parts.append(_PTY_LABEL.get(forecast.precipitation_type, "알수없음"))
        if forecast.rain_probability is not None:
            parts.append(f"강수확률 {forecast.rain_probability}%")
        if forecast.min_temperature is not None:
            parts.append(f"최저 {forecast.min_temperature}도")
        if forecast.max_temperature is not None:
            parts.append(f"최고 {forecast.max_temperature}도")
        lines.append("  ".join(parts))

    return "\n".join(lines)


@mcp.tool()
def weather_now(latitude: float, longitude: float) -> str:
    """주어진 위경도의 현재 기상 실황. 기온·습도·강수·풍속."""
    return format_observation(KMA().now(latitude, longitude))


@mcp.tool()
def weather_soon(latitude: float, longitude: float) -> str:
    """주어진 위경도의 향후 6시간 예보. 한 시간 단위."""
    return format_forecasts(KMA().soon(latitude, longitude))


@mcp.tool()
def weather_forecast(latitude: float, longitude: float) -> str:
    """주어진 위경도의 향후 3일 예보. 기온·하늘상태·강수확률."""
    return format_forecasts(KMA().forecast(latitude, longitude))


def main() -> None:
    """콘솔 스크립트 진입점."""
    mcp.run()
```

- [ ] **Step 4: 통과를 확인한다**

Run: `uv run pytest -v`
Expected: 전부 PASS. 커버리지 90% 이상.

- [ ] **Step 5: Claude에 실제로 붙인다**

`~/.claude.json`(또는 Claude Desktop 설정)에 추가한다:

```json
{
  "mcpServers": {
    "pykma": {
      "command": "uvx",
      "args": ["--from", "/Users/gimgigang/project/pykma[mcp]", "pykma-mcp"],
      "env": { "KMA_SERVICE_KEY": "발급받은 키" }
    }
  }
}
```

Claude를 다시 켜고 **"내일 서울 비 와?"**를 묻는다.
Expected: `weather_forecast` 도구가 불리고 강수확률이 답에 들어온다.
**이게 완료 기준 4번이다.**

- [ ] **Step 6: README에 MCP 절을 넣고 배포한다**

Task 7에서 뺐던 「MCP 서버」 절을 `README.md`와 `README.en.md` **양쪽에** 다시
넣는다. `args`는 로컬 경로가 아니라 배포된 패키지를 가리켜야 한다:

```json
"args": ["--from", "pykma[mcp]", "pykma-mcp"]
```

```bash
uv run ruff format && uv run ruff check && uv run ty check && uv run pytest
sed -i '' 's/version = "0.1.0"/version = "0.2.0"/' pyproject.toml
sed -i '' 's/__version__ = "0.1.0"/__version__ = "0.2.0"/' src/pykma/__init__.py
git add -A
git commit -m "feat: MCP 서버"
git push
gh release create v0.2.0 --title "v0.2.0" --notes "MCP 서버 추가."
gh run watch
```

- [ ] **Step 7: 배포된 패키지로 MCP를 다시 붙여 확인한다**

Step 5의 설정에서 `--from` 값을 로컬 경로 대신 `pykma[mcp]`로 바꾸고 Claude를
다시 켠 뒤, 같은 질문을 한다.
Expected: 동작. 이것으로 완료 기준 5개가 전부 닫힌다.

---

## 완료 후

`docs/superpowers/specs/2026-09-07-pykma-design.md` §8의 다섯 항목을 하나씩 대조한다.
전부 닫히고 **PyPI 다운로드 숫자가 찍히기 시작하면** 오픈소스 트랙 2번(에이전트
하네스/스킬, 타겟 제한 없음)의 브레인스토밍을 시작한다. 그 전에는 시작하지 않는다.
